import { ENDPOINTS } from '../endpoints';

export const about = {
  title: 'About',
  path: ENDPOINTS.ABOUT,
};

export const projects = { title: 'Projects', path: ENDPOINTS.PROJECTS };

export const experiences = {
  title: 'Experiences',
  path: ENDPOINTS.EXPERIENCES,
};

export const contact = { title: 'Contact', path: '/contact' };
