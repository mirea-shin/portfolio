{
  "name": "portfolio-project",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn dev:client & yarn dev:admin & yarn dev:server ",
    "dev:client": "yarn workspace client dev",
    "dev:admin": "yarn workspace admin dev",
    "dev:server": "yarn workspace server dev"
  }
}
